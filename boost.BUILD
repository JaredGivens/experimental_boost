cc_library(
    name = "boost",
    srcs = glob(["lib/*.so*"]),
    hdrs = glob(["include/**/*.hpp", "include/**/*.h", "include/**/*.ipp"]),
    includes = ["include"],
    visibility = ["//visibility:public"],
    linkstatic = 1,
)
